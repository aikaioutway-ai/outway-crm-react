<!--
Назначение: раздел "Менеджер" и "Кассир" (src/modules/families/) — это один
и тот же слой данных (семьи/дети/начисления/оплаты) под разными ролевыми
UI: менеджер ведёт карточки семей и вносит оплаты, кассир подтверждает или
отклоняет их и обрабатывает возвраты. Описывает иерархию дашборда (школа →
трансфер → таблица) и ключевые компоненты (FamiliesPage, CashierOverview,
DrawerUI, TabFinance).
-->
