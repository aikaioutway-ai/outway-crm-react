<!--
Назначение: журнал технического долга и предложений по оптимизации —
только записи, без автоматических правок кода. Сюда фиксируются
наблюдения вида "модуль X дублирует логику Y", "запрос Z можно
закэшировать", "именование costs/expenses/payroll стоит выровнять" —
каждая запись с датой и файлом-источником. Реализация — отдельная задача,
которую берут в работу осознанно, а не подгоняют под этот файл.
-->
