<!--
Назначение: точка входа в документацию проекта — что где искать и в каком
порядке читать. Не дублирует содержимое других файлов, только ссылки и
однострочные аннотации к ним.

Правило чтения для любой задачи (см. также CLAUDE.md в корне project/):
CLAUDE.md → project-memory.md → профильный документ по модулю/теме →
и только потом код, точечно, а не весь src/ подряд.

Порядок чтения для нового человека/сессии, если нужно охватить всё:
1. architecture.md          [READY] — стек, App.tsx-роутинг, роли → разделы
2. project-memory.md        [пусто, только структура] — подтверждённые пользователем договорённости
3. data-model.md            [READY] — таблицы Supabase (реальная схема — v2_*, не то, что раньше писал CLAUDE.md)
4. business-rules.md        [READY] — тарифы, депозит, скидка на детей, статусы оплат (пеня удалена как бизнес-правило)
5. ux-rules.md              [READY] — правила интерфейса (карточки, таблицы, навигация)
6. modules/                 — по одному файлу на функциональный кластер; заполнен только finance.md, остальные — пока заготовки
7. integrations.md          [пусто] — внешний пайплайн и Telegram-боты
8. diagrams/                [пусто, кроме README/соглашения] — те же процессы, но схемой
9. setup/                   [пусто, кроме README/индекса] — как поднять/перенести окружение
10. operations.md            [пусто] — хостинг, деплой, сезонность
11. decisions/               [пусто, кроме README/шаблона] — почему так, а не иначе
12. audit.md                 [пусто] — что стоит доделать, но пока не трогали

Статус читать буквально: READY = проверено по коду на момент заполнения, может устареть при следующих изменениях кода — если сомневаешься, сверяйся с кодом, а не только с этим файлом.

project-memory.md — долговременная память проекта: только подтверждённые
пользователем знания (архитектура, правила разработки, UX-решения,
долгосрочные бизнес-правила). Не журнал задач — для этого audit.md
(техдолг/идеи) и decisions/ (история конкретных решений с альтернативами).
Внутри разбит на разделы: Архитектурные решения / Связи между модулями /
UX-правила / Бизнес-правила / Известные ограничения / Данные и интеграции.
Правила, что и когда туда записывать — в CLAUDE.md, раздел "Память проекта
и работа между сессиями".

Новое по сравнению с первой версией документации:
- project-memory.md — долговременная память, подтверждённая пользователем.
- ux-rules.md       — единый свод правил интерфейса.
- audit.md          — журнал техдолга и идей по оптимизации, без автоправок.
- decisions/        — короткие ADR-записи по архитектурным решениям.
- setup/            — инструкции по сервисам (Supabase/Vercel/Netlify/Apps
                       Script) без токенов.
- diagrams/         — ASCII-схемы ключевых бизнес-процессов.
-->
